[build]
  command = "pip install -r requirements.txt"
  publish = "."
  functions = "netlify/functions"

[dev]
  command = "streamlit run stream_app.py"
  port = 8501

[context.production.environment]
  PYTHON_VERSION = "3.9"
